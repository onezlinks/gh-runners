# Per-project runner configuration for multi-project setups.
# Copy this file to env/<project>.env, fill it in, then add a service block in
# docker-compose.yml (see the commented example at the bottom of that file).

REPO=owner/repo
GH_PAT=
# NAME=my-project
# LABELS=
# RUNNER_GROUP=
# EPHEMERAL=false
